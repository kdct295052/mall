<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wby.mapper.StockOutDetailMapper">
    <select id="selectByOrderId" resultType="top.wby.entity.StockOutDetail">
        select * from stock_out_detail where order_id = #{orderId}
    </select>
</mapper>