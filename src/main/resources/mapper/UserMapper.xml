<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wby.mapper.UserMapper">

   <select id="pageC" resultType="top.wby.entity.User">
    SELECT * FROM user
</select>
    <select id="pageCC" resultType="top.wby.entity.User">
    select *from user ${ew.customSqlSegment}
    </select>

</mapper>
