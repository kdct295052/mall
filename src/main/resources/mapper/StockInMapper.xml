<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wby.mapper.StockInMapper">

    <select id="pageCC" resultType="top.wby.entity.StockIn">
        select *from stock_in ${ew.customSqlSegment}
    </select>
</mapper>
